{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/((?!assets|static|favicon|manifest|robots).*)", "destination": "/index.html" }
  ]
}
